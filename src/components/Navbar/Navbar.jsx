import React, { Component } from 'react';
import { Link } from 'react-router-dom';

export default Navbar;

const Navbar = (props) => {
    return (
        <>
        <Link to="/">Home</Link>
        </>
    )
}